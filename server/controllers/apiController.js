const db = require('../models/appModel');

const apiController = {};

apiController.getDeck = async (req, res, next) => {
    const query = {
        text: "SELECT" ,
        value: []
    }

}

module.exports = apiController;